<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===================== PERMISSIONS ===================== -->

    <!-- Microphone: wake word detection + STT -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Foreground service (required) + microphone type (required on targetSdk 34+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- Network: Claude API calls -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Keep CPU awake during wake word detection -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Restart after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Vibrate on wake word detection -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Notification permission (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Battery optimization exemption prompt -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Accessibility service: screen context reading -->
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE"
        tools:ignore="ProtectedPermissions" />

    <!-- ===================== APPLICATION ===================== -->

    <application
        android:name=".App"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher_round"
        android:theme="@style/Theme.Craig"
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        tools:targetApi="35">

        <!-- ============ SETUP ACTIVITY (launcher) ============ -->
        <activity
            android:name=".ui.setup.SetupActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ============ BUSINESS BRIEFING ACTIVITY ============ -->
        <activity
            android:name=".ui.onboarding.BusinessBriefingActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- ============ MAIN ACTIVITY ============ -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false"
            android:label="@string/app_name" />

        <!-- ============ ASSISTANT ACTIVITY ============
             Handles:
             - Home long-press (ACTION_ASSIST) → replaces Gemini
             - Wake word activation (started by WakeWordService)
             - Manual tap from MainActivity
        -->
        <activity
            android:name=".ui.conversation.AssistantActivity"
            android:exported="true"
            android:showOnLockScreen="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">

            <!-- Long-press home button: replaces Gemini/Google Assistant -->
            <intent-filter>
                <action android:name="android.intent.action.ASSIST" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- Voice command intent (some OEM launchers use this) -->
            <intent-filter>
                <action android:name="android.intent.action.VOICE_COMMAND" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- Required metadata for RoleManager.ROLE_ASSISTANT eligibility -->
            <meta-data
                android:name="android.intent.action.ASSIST"
                android:value="true" />
        </activity>

        <!-- ============ WAKE WORD FOREGROUND SERVICE ============ -->
        <service
            android:name=".service.WakeWordService"
            android:exported="false"
            android:foregroundServiceType="microphone"
            android:stopWithTask="false" />

        <!-- ============ ACCESSIBILITY SERVICE ============ -->
        <service
            android:name=".service.ContextAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Disable WorkManager default auto-init; App.kt provides Configuration.Provider -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- ============ BOOT RECEIVER ============ -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
